# Forward every non-pkgreg query through the resolver Docker supplies to this
# container. The instance-specific address= rule is passed by Compose so secrets or
# host-specific addresses never need a generated config file.
domain-needed
bogus-priv
no-hosts
cache-size=1000
log-facility=-
user=nobody
