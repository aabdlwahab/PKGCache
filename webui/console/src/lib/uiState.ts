// Client-only UI state (persisted to localStorage as pcc_theme / pcc_mode).
export type Theme = "dark" | "light";
export type Mode = "online" | "offline";
export type SortKey = "name" | "size" | "date" | "ver";
